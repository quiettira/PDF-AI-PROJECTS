# Python pdf-ai-summarizer environment template
# Copy this file to .env (optional). FastAPI uses python-dotenv load_dotenv().
# Windows PowerShell:
#   Copy-Item ENV_TEMPLATE.txt .env

# Allow frontend origin(s)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: enable Gemini provider by setting your API key
# If empty/unset, the app uses mock provider.
GEMINI_API_KEY=
